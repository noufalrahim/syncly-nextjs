export enum EUrl {
  home = "/",
  task = "/task",
  settings = "/settings",
  projectSettings = "/projects/:projectId/settings",
  members = "/members",
  notes = "/notes",
  inbox = "/inbox",
  notification = "/notification",
  login = "/login",
  default = "",
  project = "/projects/:projectId",
}
